{
  "version": 1,
  "lastUpdated": "2026-08-12",
  "labels": [
    "Local only",
    "External AI optional",
    "External AI required",
    "Sensitive",
    "Public-data only"
  ],
  "dataCategories": [
    "job_posting",
    "public_metadata",
    "resume",
    "salary_floor",
    "private_notes",
    "application_history",
    "career_goals",
    "location_preferences"
  ],
  "features": [
    {
      "id": "job-tracking",
      "name": "Job tracking",
      "labels": ["Local only", "Sensitive"],
      "dataCategories": ["job_posting", "public_metadata", "private_notes"],
      "defaultDataFlow": "Stored locally in SQLite. No external AI or external channel is required.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Core tracking works locally."
      },
      "sensitivityNote": "Saved jobs and notes can reveal employment status, career goals, and job-search urgency."
    },
    {
      "id": "saved-searches",
      "name": "Saved searches",
      "labels": ["Local only", "Sensitive"],
      "dataCategories": ["career_goals", "location_preferences"],
      "defaultDataFlow": "Stored locally in SQLite. Searches run through configured local source settings.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Saved searches work locally."
      },
      "sensitivityNote": "Search terms and locations can reveal career goals and relocation constraints."
    },
    {
      "id": "application-kanban",
      "name": "Application Kanban",
      "labels": ["Local only", "Sensitive"],
      "dataCategories": ["application_history", "private_notes"],
      "defaultDataFlow": "Stored locally in SQLite. Optional external channels are user-configured only.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Application tracking works locally."
      },
      "sensitivityNote": "Application stages, reminders, and notes are private job-search records."
    },
    {
      "id": "application-assist",
      "name": "Application Assist",
      "labels": ["Local only", "Sensitive"],
      "dataCategories": ["resume", "application_history", "career_goals"],
      "defaultDataFlow": "Profile details and selected resume file copies stay local. Resume selection uses the app file picker and app screens receive only a private saved-file reference plus display name.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Application Assist prepares local profile details without external AI. Resume attachment remains manual."
      },
      "sensitivityNote": "Profile details and resumes are sensitive. Do not expose raw local paths to app screens or upload saved resumes automatically."
    },
    {
      "id": "first-seen-last-seen-job-tracking",
      "name": "First-seen and last-seen job tracking",
      "labels": ["Local only", "Public-data only"],
      "dataCategories": ["job_posting", "public_metadata"],
      "defaultDataFlow": "Stored locally from monitored public or official job sources.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Posting age tracking works locally."
      },
      "sensitivityNote": "Uses posting metadata rather than private candidate records."
    },
    {
      "id": "employer-dossier",
      "name": "Employer dossier",
      "labels": ["Local only", "Sensitive"],
      "dataCategories": ["job_posting", "public_metadata", "application_history"],
      "defaultDataFlow": "Creates or reuses the job's local Opportunity Case and builds a read-only dossier from the saved posting, installed reviewed source metadata, and count-only application, interview, offer, and terminal-outcome history for the exact saved employer name. Opening the dossier performs no network request. Opening a saved posting is a separate visible browser action that discloses the destination URL to that site.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Employer research remains available from dated local evidence while offline."
      },
      "sensitivityNote": "Employer names and aggregate outcome history can reveal a user's job-search activity. The dossier does not expose notes, contacts, resume content, offer amounts, salary floors, or application text."
    },
    {
      "id": "user-configured-job-source-endpoints",
      "name": "User-approved job-source feeds",
      "labels": ["Sensitive"],
      "dataCategories": ["career_goals", "location_preferences"],
      "defaultDataFlow": "JobsWithGPT scheduled contact is disabled while provider endpoint and usage-policy review is unresolved. Configured details, prior exact approval, and minimized contact history remain local for inspection or removal. A future reviewed feed would also require a current exact local payload approval.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Leave the job-source feed blank and use local, official, or user-opened source paths."
      },
      "sensitivityNote": "Search titles and locations can reveal career goals. The scheduler must skip JobsWithGPT while provider governance is unresolved, regardless of a saved payload approval. The local contact-history ledger must not store raw titles, raw location, resumes, salary floors, private notes, application history, screening answers, profile details, or full source links."
    },
    {
      "id": "ghost-job-heuristic-scoring",
      "name": "Ghost-job heuristic scoring",
      "labels": ["Local only", "Public-data only"],
      "dataCategories": ["job_posting", "public_metadata"],
      "defaultDataFlow": "Scored locally from public posting signals, source history, and local recency data.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Heuristic ghost-job scoring works locally."
      },
      "sensitivityNote": "Default scoring uses job-posting content and local source metadata."
    },
    {
      "id": "ghost-job-external-ai-explanation",
      "name": "Ghost-job explanation with external AI",
      "labels": ["External AI optional", "Public-data only"],
      "dataCategories": ["job_posting", "public_metadata"],
      "defaultDataFlow": "Local explanation is preferred. Optional external AI may receive minimized public posting data only after preview and approval.",
      "externalAi": {
        "allowed": true,
        "required": false,
        "fallback": "Show local heuristic reasons without sending data."
      },
      "sensitivityNote": "Do not include resume, salary floor, private notes, or application history."
    },
    {
      "id": "job-description-summary",
      "name": "Job description summary",
      "labels": ["External AI optional", "Public-data only"],
      "dataCategories": ["job_posting", "public_metadata"],
      "defaultDataFlow": "Job cards show the original posting details locally. The shipped Outside AI summary action is optional, starts from the job card, and may receive minimized public posting details only after preview, edit, and approval.",
      "externalAi": {
        "allowed": true,
        "required": false,
        "fallback": "Show the original job card, local fit signals, posting-risk cues, and any extracted public fields without sending data."
      },
      "sensitivityNote": "Public-data-only summaries must not include unrelated candidate data."
    },
    {
      "id": "resume-assistance-application-readability",
      "name": "Resume assistance and application readability",
      "labels": ["Local only", "Sensitive"],
      "dataCategories": ["resume", "job_posting", "career_goals"],
      "defaultDataFlow": "Resume parsing, explicit readable-text preview and copy, reviewed-skill sorting, section review, readable export checks, and truthful edit guidance work locally by default. Job posting text may be compared with a selected resume only after the user chooses that resume, and reviewed resume skills influence sorting only after the user turns that preference on.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Show local readable-text preview, application readability checks, and user-editable fit notes."
      },
      "sensitivityNote": "Resume text and career goals are sensitive. Do not send resume content, salary floors, private notes, or application history outside the app unless a separate optional external-AI workflow is explicitly selected, previewed, redacted, and approved."
    },
    {
      "id": "resume-job-fit-explanation",
      "name": "Resume/job fit explanation",
      "labels": ["External AI optional", "Sensitive"],
      "dataCategories": ["job_posting", "resume", "career_goals", "salary_floor"],
      "defaultDataFlow": "Local scoring and readable fit signals should work without a provider. Optional external AI requires explicit sensitive-payload selection, preview, redaction, and approval.",
      "externalAi": {
        "allowed": true,
        "required": false,
        "fallback": "Show local fit score, transparent reasons, and user-editable application readability notes."
      },
      "sensitivityNote": "Resumes, salary floors, and career goals are sensitive and must not be sent by default."
    },
    {
      "id": "negotiation-prep",
      "name": "Negotiation prep",
      "labels": ["External AI optional", "Sensitive"],
      "dataCategories": ["job_posting", "salary_floor", "career_goals", "private_notes"],
      "defaultDataFlow": "Local prompts and checklists should work without a provider. Optional external AI requires explicit sensitive-payload selection, preview, redaction, and approval.",
      "externalAi": {
        "allowed": true,
        "required": false,
        "fallback": "Show local compensation questions, salary-floor reminders, and written-range checks."
      },
      "sensitivityNote": "Negotiation notes can reveal salary expectations, urgency, and personal constraints."
    },
    {
      "id": "salary-floor-protection",
      "name": "Salary floor protection",
      "labels": ["Local only", "Sensitive"],
      "dataCategories": ["salary_floor", "career_goals"],
      "defaultDataFlow": "Salary floors stay local by default and are used for local filtering, warnings, and score explanations.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Salary-floor filtering and below-floor warnings work locally."
      },
      "sensitivityNote": "Salary floor and role constraints are sensitive user data."
    },
    {
      "id": "salary-transparency-check",
      "name": "Salary transparency check",
      "labels": ["Local only", "Public-data only"],
      "dataCategories": ["job_posting", "public_metadata"],
      "defaultDataFlow": "Check listed pay range, range width, missing criteria, and public posting signals locally.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Transparency checks work locally from posting text and metadata."
      },
      "sensitivityNote": "Default check uses public posting data only."
    },
    {
      "id": "salary-transparency-ai-explanation",
      "name": "Salary transparency explanation with external AI",
      "labels": ["External AI optional", "Public-data only"],
      "dataCategories": ["job_posting", "public_metadata"],
      "defaultDataFlow": "Optional external AI may explain public pay-transparency signals only after payload preview and approval.",
      "externalAi": {
        "allowed": true,
        "required": false,
        "fallback": "Show local pay-transparency reasons."
      },
      "sensitivityNote": "Do not include the user's salary floor unless the feature is reclassified as Sensitive and explicitly approved."
    },
    {
      "id": "safe-support-report",
      "name": "Safe support report",
      "labels": ["Local only", "Sensitive"],
      "dataCategories": ["public_metadata", "application_history"],
      "defaultDataFlow": "Generated and sanitized locally. User chooses whether to copy, save, or attach the report.",
      "externalAi": {
        "allowed": false,
        "required": false,
        "fallback": "Support report generation works locally."
      },
      "sensitivityNote": "Support reports may summarize app state and must redact names, emails, secrets, paths, URLs, private notes, and raw application content."
    },
    {
      "id": "research-evaluation",
      "name": "Research and evaluation",
      "labels": ["External AI optional", "Public-data only"],
      "dataCategories": ["job_posting", "public_metadata"],
      "defaultDataFlow": "Use public job postings and synthetic candidate profiles by default.",
      "externalAi": {
        "allowed": true,
        "required": false,
        "fallback": "Run local or synthetic-data evaluation without external AI."
      },
      "sensitivityNote": "Real user resumes, salary floors, private notes, application history, or PII-heavy datasets require explicit informed consent."
    }
  ]
}
