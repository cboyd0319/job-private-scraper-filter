# Technical Debt Tracker

Use this file for durable debt that affects agent reliability, architecture,
security, or developer workflow.

## Open Items

No open items.

## Closed Items

| ID | Area | Evidence | Outcome | Closed |
| -- | ---- | -------- | ------- | ------ |
| DRY-002 | Small dependency review | `docs/harness/evidence/v3-milestone-0-repository-truth-2026-07-18.json` records the focused dependency audit and checks. | Replaced the sole direct `scopeguard` use and custom UUID directory cleanup with the already-owned `tempfile` API. Retained `hex`, `urlencoding`, and `basic-toml` because custom replacements would duplicate security-sensitive encoding, URL component semantics, or nested TOML parsing. | 2026-07-18 |
| DRY-003 | Frontend source duplication | Fresh `npm run lint:dup -- --list` evidence is recorded in `docs/harness/evidence/v3-milestone-0-repository-truth-2026-07-18.json`. | Closed the stale tracker item after all six maintained production and test scopes measured zero exact duplicate runs under the 14-significant-line contract. Semantic duplication remains subject to normal review. | 2026-07-18 |
| DRY-001 | Rust crate source duplication | `docs/harness/evidence/crates-dry-completion-2026-07-16.json` records the full remediation. `npm run lint:dup -- --list` reports zero maintained production and test duplication under `crates/`, down from 693 production lines across 35 regions and 2,184 test lines across 79 regions. | Shared security, domain, storage, source, document, resume, and test-support behavior now has canonical owners. Both crate baselines are ratcheted to zero. The distinct frontend debt was split into DRY-003 instead of being falsely closed. | 2026-07-16 |
| HE-019 | Harness false positive on local `.claude` | `npm run harness:check` failed on an otherwise clean working tree whenever a local gitignored `.claude/settings.local.json` was present, because `scripts/harness/checks/repo-artifacts.mjs` both root-flagged and forbade `.claude` even though the harness repository-file policy already ignored it. | Added `.claude` to `ignoredTraversalPaths` and removed it from `forbiddenArtifactDirs` in `repo-artifacts.mjs`, with a comment explaining it is a legitimate local override surface and a focused test in `scripts/tests/harness/check-repo-artifacts.test.mjs` asserting both the root-entry and filesystem-bloat checks ignore it. `.gitignore` still prevents tracking the directory. | 2026-06-24 |
| HE-017 | Run-evidence ledger | Deep harness audit on 2026-05-31 found no compact current evidence ledger with command, scope, exit code, and date, so completion claims required reading long plans and chat. | Replaced the pre-standard ledger with revision-bound evidence files under `docs/harness/evidence/`; the historical ledger is archived at `docs/harness/archive/evidence-log-pre-standard-2026-07-14.md`. | 2026-07-14 |
| HE-018 | Harness architecture map and sensor registry | Deep harness audit on 2026-05-31 (Documentation P1 "Add harness architecture map" and P2 "Add sensor ownership comments") found harness rules spread across docs, scripts, workflows, and plans with no map of which command owns which rule. | Added `docs/harness/harness-map.md`, a topology table plus a sensor registry naming each command's area, reason, source, and retire condition, and the named check modules. Referenced from `AGENTS.md` and `agent-operating-model.md` and locked by a manifest snippet. | 2026-06-24 |
| SEC-002 | Rust advisory watch | `cargo audit` on 2026-06-19 exits 0 with no vulnerabilities and 18 allowed warnings. `cargo deny check advisories bans licenses sources` exits 0. Extra-strict `cargo audit --deny warnings` still exits 1 on informational warnings: GTK3/glib through Tauri's Linux WebKit stack, `unic-*` through Tauri `urlpattern`, `paste` through optional ML/Candle/tokenizer dependencies, and `proc-macro-error` through proc-macro dependencies. The 2026-06-19 dependency slice removed unused `tokenizers` HTTP support, dropping duplicate downloader/TLS paths while `cargo check --features embedded-ml` and targeted `cargo test --features embedded-ml core::ml` pass. `cargo update --dry-run --verbose` reports zero compatible lockfile updates; remaining absolute-latest gaps are the SQLx-constrained `libsqlite3-sys` pin plus four upstream transitive pins. | Closed as a release blocker because no vulnerable Rust advisory or compatible update remains locally actionable. Reopen if `cargo audit` reports a vulnerability, `cargo deny` fails, compatible lockfile updates appear, or Tauri/Candle/tokenizers upstream releases remove the warning paths. | 2026-06-19 |
| HE-009 | Sensor modularity | `scripts/checks/repo-bloat.mjs` is a tested thin orchestrator for product policy, repository policy, and focused modules under `scripts/harness/checks/`. Script tests are recursively discovered, `npm run test:scripts` and `npm run harness:check` pass, and the harness score is `100/100`. | Closed as a release blocker; future modularity work should target a specific oversized module or unclear ownership boundary instead of keeping broad cleanup open. | 2026-07-13 |
| AUD-001 | Broad-audience fixture drift | Broad-audience audit on 2026-05-31 found company suggestions, mocks, E2E examples, config comments, source defaults, source recommendations, synonym groups, and backend fixtures skewed toward software/tech roles. Current proof: broad-audience script tests `13/13` pass, `npm run lint:bloat` passes, and the isolated broad-audience fixture sensor covers profile seeds, mocks, source defaults, source recommendations, synonym groups, backend fixtures, salary examples, scraper examples, and docs examples. | Closed as a release blocker; future broad-audience drift should be opened as a specific fixture or sensor gap. | 2026-06-19 |
| UX-003 | Agent-aided plain-language copy audit | Agent-aided copy cleanup changed Settings, support-history export, notification docs, credentials security docs, privacy/AI policy docs, market docs, user-data docs, application-tracking docs, and resume-matcher docs from technical setup and policy wording to plain local-troubleshooting language. Current focused proof: product-copy script tests `59/59` pass and `npm run lint:bloat` passes. | Closed as a release blocker; future plain-language drift is covered by focused product-copy and bloat sensors. | 2026-06-19 |
| UX-002 | Zero-technical UX audit | The zero-technical audit fixed setup, Settings, import, notification, source status, support report, application help, resume, pay, and recovery copy across user-facing surfaces. Current focused proof: product-copy script tests `59/59` pass, `npm run lint:bloat` passes, and the v2.9.0 whole-UI ledger records Settings, setup, import, support, keyboard, empty/loading/error, desktop, and mobile verification. | Closed as a release blocker; future findings should be opened as specific copy or flow regressions instead of a broad incomplete audit. | 2026-06-19 |
| BLOAT-003 | Removing bloat and junk | User reported root clutter and likely nested junk after the disposable-artifact sensor was already passing. The current sweep found no tracked disposable artifacts beyond legitimate source/test paths, confirmed root and docs roadmap/ML pages have distinct maintained roles, and `npm run lint:bloat` passes. | Closed as a release blocker; future bloat should be opened as a specific failing gate or stale-file finding instead of broad cleanup. | 2026-06-19 |
| HE-012 | Lifecycle snapshot | Five-tuple audit on 2026-06-01 scored lifecycle lowest because agents had no one-command restart snapshot for branch state, active plans, harness size, test-module counts, or next-best work. | `npm run harness:session -- --json` now reports branch state, latest commit, active plans, indexed workstreams, harness size, test-module counts, score, and next-best work; `scripts/tests/harness/harness-session.test.mjs` and current harness score keep it covered. | 2026-06-19 |
| SEC-020 | Release SBOM and provenance | CheatSheetSeries supply-chain guidance recommended publishing SBOMs and provenance for release artifacts. | Added `npm run release:sbom` for SPDX 2.3 release SBOM generation from npm and Cargo lockfiles, release asset digest manifests, GitHub `actions/attest` provenance and SBOM attestations in `release.yml`, public SBOM and attestation verification in `verify-release-artifacts.yml`, focused script tests, and security-sensor coverage so the release supply-chain gates cannot silently disappear. | 2026-06-18 |
| SEC-019 | Inspectable optional source endpoints | Security audit on 2026-06-01 found that a configured JobsWithGPT source endpoint could send saved job titles, location, remote preference, and result limit during scheduled source checks, and later leave users unsure when an approved source last ran. | JobsWithGPT now requires a locally approved exact request before scheduled checks, skips changed or unapproved details, logs high-level metadata only, records a local minimized source-request ledger, and shows Settings users the latest approved contact time plus host/count/outcome summary. The ledger stores source id, host only, title count, location-present boolean, remote-only boolean, limit, timestamp, and outcome, not raw titles, raw location, resumes, salary floors, notes, application history, or full source links. Source-boundary, Settings, Rust health, and scheduler tests cover drift. | 2026-06-02 |
| SEC-015 | Screening-question debug logs | Security audit on 2026-06-01 found Application Assist debug logs could record raw employer screening-question text and saved answer match patterns while matching screening answers. | Application Assist traces and screening-answer command logs now keep raw question text, saved patterns, original answers, and edited answers out of logs, using character counts, IDs, and booleans instead. Safe support reports redact labeled screening questions/answers, IPC responses omit raw answer-history data, and privacy and IPC harness plus focused Rust response tests cover drift. | 2026-06-02 |
| SEC-018 | Frontend support-report sensitive context redaction | Privacy follow-up on 2026-06-01 found renderer-side error reports already redacted credentials, URLs, emails, and local paths, but did not explicitly classify job-search content keys such as salary floors, resume text, private notes, application history, screening answers, location preferences, career goals, and personal circumstances as sensitive. | Expanded `SENSITIVE_KEY_PATTERN` and labeled-text sanitization for sensitive job-search context before local persistence, dev console forwarding, and exported support reports. Added focused Vitest coverage and privacy-logging harness checks so the categories stay enforced. | 2026-06-01 |
| SEC-014 | ATS timeline event text minimization | Security audit on 2026-06-01 found application note updates and reminder creation duplicated private note bodies and reminder messages into `application_events.event_data`, creating hidden extra copies beyond the user-visible owning tables. | Timeline event payloads now keep only presence/count metadata for notes and reminder messages. Private note text remains in `applications.notes`; private reminder text remains in `application_reminders.message`. Added a migration to scrub legacy timeline payloads and privacy-logging coverage blocking raw note/reminder event-data drift. | 2026-06-01 |
| SEC-013 | Screening-answer history IPC minimization | Security audit on 2026-06-01 found `get_answer_statistics` returned raw historical screening questions, original answers, and edited answers, and suggestion source metadata returned raw saved patterns or original historical questions to the renderer. | Minimized automation answer-learning IPC so renderer responses keep only summary counts, timestamps, source type, and non-sensitive IDs. Raw historical questions, answer text, edited answer text, and saved patterns stay backend-local. Added Rust response tests and IPC-minimization sensors blocking raw answer-history IPC and renderer answer-history management calls. | 2026-06-01 |
| SEC-012 | External output boundary hardening | Security audit on 2026-06-01 found renderer CSP allowed direct external `connect-src` hosts, saved job URLs used a string prefix guard, Application Assist opened raw job URLs before loading profile-backed form filling, and alert email HTML rendered scraped job fields into HTML and `href` attributes without escaping or validation. | Restricted renderer CSP to `connect-src 'self'`, routed saved-job storage and Application Assist browser launch through shared public HTTPS job-destination validation, escaped notification email HTML text/attribute values, validated job links before creating email `href` attributes, and added security sensor coverage against external renderer connect hosts returning. | 2026-06-01 |
| HE-016 | Rule 0 PR review evidence | Deep harness audit on 2026-05-31 found that `.github/PULL_REQUEST_TEMPLATE.md` had a privacy checklist but did not name Rule 0, external AI gateway routing, payload preview, broad audience, or zero-technical-skill support. | Updated the PR template with Rule 0, local-first workflows, external AI gateway, payload preview/redaction/cancel/approval/logging, responsible-use, safe-support-report, broad-audience, and zero-technical-knowledge review checks. Added manifest snippets so `npm run harness:check` fails if the template drifts. | 2026-06-01 |
| SEC-010 | Browser companion local server boundary | Security audit on 2026-06-01 found the browser companion local HTTP server used a reusable token and wildcard CORS for browser-origin POSTs. | Removed wildcard CORS and custom auth headers. Browser actions now use a structured `no-cors` text envelope with an exact operation, confirmed public HTTPS origin, one-use secret held only in Rust memory and the copied button, atomic consumption, replacement and stop revocation, and a ten-minute expiry. Rust activates a replacement only after clipboard copy succeeds, rechecks installed source authority before queueing and saving, and rejects replay, origin drift, restricted domains, credentials, hidden state, and oversized requests. | 2026-07-19 |
| SCRAPE-003 | Process-wide source cooldowns | Security and reliability audit on 2026-06-01 found manual scraper runs created fresh scheduler/scraper instances, so per-scraper `RateLimiter` buckets could reset between runs. | Added a process-wide shared `RateLimiter` for production scraper constructors while preserving isolated `RateLimiter::new()` for tests. Added focused coverage proving shared handles reuse exhausted bucket state across instances. | 2026-06-01 |
| SEC-011 | Bookmarklet import validation reuse | Security audit on 2026-06-01 found bookmarklet imports still entered through local server parsing and raw insert logic instead of the same URL, length, and job-field validation path used by normal imports. | Bookmarklet job data now rejects non-public or non-HTTPS job links before storage and saves through `Database::upsert_job`, so shared job field length and URL protocol validation covers the local browser-helper path. Added focused tests for unsafe URLs, overlong titles, and valid storage. | 2026-06-01 |
| SEC-016 | Renderer-trusted resume upload path | Security audit on 2026-06-01 found Application Assist could trust a renderer-supplied `resume_file_path` and pass it to browser file upload, allowing a compromised renderer to upload arbitrary local files. | Resume selection now goes through a backend native file-picker command, copies selected files into app-owned local storage, sends the renderer only a token and display name, rejects renderer-supplied resume paths on save, validates legacy saved paths before form prep, and keeps resume attachment manual so saved resumes are not uploaded automatically. Focused Rust, UI, and IPC-minimization harness tests cover the boundary. | 2026-06-01 |
| SEC-017 | Renderer-trusted resume import paths | Security audit on 2026-06-01 found resume upload/import commands could read renderer-supplied local file paths. | PDF upload and JSON Resume import now use backend native file-picker commands, raw renderer-supplied resume paths are rejected, PDF uploads are copied into app-owned local storage before parsing, JSON import keeps file type and size checks in Rust, and source-quality coverage rejects renderer-owned file picker imports. | 2026-06-01 |
| SEC-009 | User-controlled external URL fetch boundary | Security audit on 2026-06-01 found user-controlled external URL validation rejected literal private IPs but not private hostname suffixes, embedded private IP hostnames, DNS-resolved private targets, or scraper redirects. | Hardened shared URL validation, added fetch-time DNS resolution checks for user-entered job imports and JobsWithGPT, disabled redirects in shared scraper clients and JobsWithGPT smoke checks, and added focused Rust regression tests. | 2026-06-01 |
| HE-015 | E2E runtime budget tracking | Deep harness audit on 2026-05-31 found that Playwright timings lived only in prose and terminal output, so slow-test drift could return unnoticed. | Added `scripts/dev/e2e-budget.mjs`, focused parser/budget tests, `npm run test:e2e:smoke:budget`, and `npm run test:e2e:all:budget`. The smoke budget command measures Playwright JSON output and fails on unexpected tests, duration over budget, or test-count drift. | 2026-06-01 |
| HE-014 | Feature privacy label manifest | Deep harness audit on 2026-05-31 found that feature privacy labels existed in prose and AI gateway types but not in a machine-readable repo-wide manifest. | Added `docs/harness/feature-privacy-labels.json` and harness validation for labels, data categories, sensitive-label alignment, external-AI allowance, required local fallback guidance, and required core feature entries. | 2026-06-01 |
| HE-013 | Diff-aware verification planning | Deep harness audit on 2026-05-31 found that agents had to manually translate changed files into verification commands from `docs/harness/verification-matrix.md`. | Added `npm run harness:plan -- --since origin/main`, `scripts/harness/plan.mjs`, and focused script tests so changed files produce matrix-backed command suggestions for docs, scripts, frontend, Playwright, Rust, Tauri invokes, security/privacy, package, and user-facing copy surfaces. | 2026-06-01 |
| SEC-007 | Resume file path exposure | Security audit on 2026-05-31 found Application Assist stored `resume_file_path` in SQLite and returned/displayed the raw path through profile IPC and UI. | Changed profile IPC/UI to expose only `hasResumeFile` plus `resumeFileName`, then added a backend-owned token boundary for resume selection so the renderer never receives or submits raw local paths. Resume attachment remains manual. | 2026-05-31 |
| SEC-006 | Bookmarklet import token IPC | Security audit on 2026-05-31 found `get_bookmarklet_config` returned the raw bookmarklet auth token to the renderer, and the frontend embedded it in copied browser-button code. | Removed the token from the bookmarklet config DTO and mocks, added a backend `copy_bookmarklet_code` command that writes the browser-button code to the OS clipboard without exposing the token to React state, and added an IPC-minimization sensor for token DTO drift. | 2026-05-31 |
| SEC-008 | Support-report sanitizer coverage | Security audit on 2026-05-31 found Rust support-report sanitization did not redact generic URLs or common `access_token`, `refresh_token`, `password`, and bookmarklet token forms before copied or saved reports could be shared. | Hardened the Rust sanitizer for generic URLs, query secrets, password-like values, and `X-JobSentinel-Token` fields, with focused sanitizer tests. | 2026-05-31 |
| SEC-005 | IPC payload minimization | Read-only security audit on 2026-05-31 found non-settings screens could request full application profile payloads, import URLs preserved raw pasted data, and Dashboard read full config for two fields. | Added `has_application_profile`, `get_application_profile_preview`, `get_dashboard_preferences`, canonical import URL minimization before preview/hash/storage, `{ jobId }` import responses, focused tests, and harness sensors for wrapper invokes and import mock return shape. | 2026-05-31 |
| UX-001 | Zero-technical support and recovery | Read-only UX audit on 2026-05-31 found `SubmitOptions` and Quick Start still center GitHub/Drive issue paths, settings crash recovery lacked a safe support report action, problem-history/export labels exposed technical details, setup docs mentioned notification providers beyond the wizard, and generated reports used system/config/structured-data language. | Made local safe support report saving the recommended feedback submit path, kept GitHub optional, added safe support report copy/save actions to modal crash recovery, aligned Quick Start setup wording, simplified problem-history/export labels, made saved-report success steps account-optional, and reworded generated reports with plain support labels. | 2026-05-31 |
| SEC-004 | Privacy logging audit slice | Read-only security audit on 2026-05-31 found raw automation dropdown values in Rust tracing and raw frontend `console.error` forwarding/default browser logging. | Removed raw selected values and selectors from automation dropdown logs, sanitized forwarded console arguments, suppressed raw global default console output after capture, and added bloat plus Vitest regression coverage. | 2026-05-31 |
| HE-011 | Active plan compaction | Active plans contained long historical progress tables and the handoff could lag current local commit state. | Added `docs/plans/active/status.md` as the compact restart surface, archived older progress rows in `docs/plans/archive/progress-history-2026-05-28-to-2026-05-29.md`, and routed the plan index, harness guide, agent guide, and handoff toward the compact state while keeping detailed plans authoritative. | 2026-05-31 |
| HE-008 | Harness policy manifest | Deep harness audit on 2026-05-31 found hardcoded policy and duplicate owners. | Replaced the old manifest and dead duplicate checkers with canonical `scripts/harness/contracts/harness.json`; `scripts/checks/harness.mjs` delegates to the shared semantic contract. | 2026-07-14 |
| HE-010 | Environment doctor platform coverage | Deep harness audit on 2026-05-31 found `npm run doctor` did not check Linux Tauri system packages, Playwright browser launch readiness, or local toolchain drift from owned baselines. | Added Linux `pkg-config` checks for WebKitGTK, GTK, appindicator, and librsvg plus `patchelf`; added Playwright Chromium launch readiness; added `npm run doctor:e2e` as a strict E2E gate; and added Node/Rust owner-baseline drift warnings with script coverage. | 2026-07-14 |
| SEC-003 | External AI provider surface | Deep harness audit on 2026-05-31 found provider detection was narrow and mostly code-only. | Added `npm run lint:external-ai` and wired it into `npm run harness:check`; the sensor scans code plus JSON, YAML, TOML, and env-style config for external AI endpoints, SDK imports, dependency declarations, hosted inference endpoints, and provider API-key variables outside `src/shared/externalAi/`. | 2026-05-31 |
| HE-007 | CI harness coverage | Deep harness audit on 2026-05-31 found normal CI lacked `npm run harness:check` and `npm run test:scripts`. | Prior CI coverage was retired on 2026-07-14 under the explicit `pre-alpha-private-no-ci` user exception. Local gates remain, but the canonical shared-history enforcement requirement is knowingly unmet until the exception retires. | 2026-07-14 |
| REL-001 | Release preflight | Tag and manual build workflows built and uploaded artifacts without first running repo harness checks or version/tag consistency checks. | Added release-version validation across `package.json`, Tauri config, and Cargo metadata; gated release builds with harness, script-test, markdown, frontend-build, Rust fmt, clippy, and unit-test preflight. On 2026-06-17, consolidated manual Windows/Linux packaging into `release.yml` dispatch instead of separate workflows. | 2026-06-17 |
| SEC-001 | Rust transitive dependency alert | Dependabot alert #67 for `rand` 0.7.3 was accepted temporarily on 2026-05-28 because it entered only through the upstream Tauri build-time chain. The latest-stable dependency refresh removed the `rand` 0.7 and `rsa` advisory paths from `Cargo.lock`; `cargo audit` exits 0 with only allowed warnings. | Closed the exception by upgrading the Rust dependency graph to current stable Tauri and related crates instead of carrying the dismissed alert forward. | 2026-05-28 |
| BLOAT-001 | Removing bloat and junk | Root and nested paths needed repeat manual sweeps for disposable artifacts, while earlier tracked-file audits had already found generated output outside root. | Added `npm run lint:bloat` and wired it into `npm run harness:check` so disposable local artifacts, tracked generated output, logs, temp files, database files, installer packages, and AI-generated root summary files fail deterministically; current repo scan passes with only expected local caches ignored. | 2026-05-20 |
| SCRAPE-001 | Scraper retry coverage | Several source adapters called `reqwest` directly for custom headers, query parameters, JSON bodies, or API auth clients instead of shared retry helpers. | Added shared custom-request retry helpers, routed source adapters through them, and preserved source-specific response handling for anti-bot, auth, and schema fallback cases. | 2026-05-19 |
| BLOAT-002 | Duplicate docs screenshot artifacts | `tests/e2e/docs/images/` contained tracked copies of documentation screenshots while `README.md` and screenshot tooling use canonical `docs/images/`. | Removed the stale tracked screenshot bundle and ignored `/tests/e2e/docs/` so future Playwright artifacts do not re-enter the repo. | 2026-05-19 |
| QA-005 | Testing docs drift | Active testing docs still demonstrated runtime Playwright skips and focused-test source modifiers after skip-sprawl cleanup. | Replaced those examples with command-line focused-run guidance and added bloat coverage so maintained testing docs cannot reintroduce runtime skip or focused-test modifier patterns. | 2026-05-20 |
| QA-004 | E2E skip sprawl | Playwright suites contained many runtime `test.skip()` branches that allowed stale page objects and removed UI flows to pass without exercising behavior. | Replaced skip-heavy suites with current mock-backed assertions across job interactions, filtering, application tracking, settings, resume, market intelligence, Application Assist, resume builder, and keyboard navigation; `tests/e2e/playwright` now has no `test.skip()` or `.only` matches. | 2026-05-20 |
| SCRAPE-002 | Rate limiter contention | `RateLimiter::wait` held the shared bucket mutex while sleeping for token refill. | Released the lock before sleeping and added regression coverage so one exhausted bucket cannot block unrelated scrapers. | 2026-05-19 |
| HE-004 | Tauri invoke map | `scripts/checks/tauri-invokes.mjs` verifies production frontend command calls against the private command registry through `npm run harness:check`. | Frontend/backend IPC drift now fails locally before broken workflows ship. | 2026-07-13 |
| HE-005 | Tracked generated output | `src-tauri/coverage/` was committed despite generated coverage output being ignored. | Removed tracked coverage artifacts from repo history tip; future coverage output remains ignored by `.gitignore`. | 2026-05-19 |
| HE-006 | Stale migration docs | Unlinked migration and summary docs duplicated authoritative references and contained hardcoded implementation/test claims. | Removed stale docs, narrowed root-only generated-doc ignore rules, and replaced the undo note with a compact completed plan. | 2026-05-19 |
| QA-003 | Test quality sensors | `scripts/checks/test-quality.mjs` rejects no-op `expect(true)`, always-true fallbacks, focused `.only` tests, and skipped unit tests through `npm run harness:check`. | Repeated weak assertion drift and hidden skipped component tests now have a mechanical guard. | 2026-05-19 |
| HE-003 | Security sensors | `scripts/checks/security-sensors.mjs` verifies required security docs, security matrix coverage, hosted release gates, and local security boundaries through `npm run harness:check`. | Local harness summarizes security-specific gates so agents get a deterministic threat-surface reminder. | 2026-07-14 |
| HE-001 | Harness docs | `scripts/harness/contract.mjs` rejects stale owners, retired paths, invalid state, and incomplete verification policy. | Maintained docs route to current owners and retired duplicate checkers cannot silently return. | 2026-07-14 |
| HE-002 | Architecture sensors | `scripts/checks/frontend-boundaries.mjs` enforces production frontend import boundaries and runs through `npm run harness:check`. | Shared frontend layers now have a mechanical drift sensor. | 2026-05-19 |
| QA-002 | E2E assertion quality | Search for always-true expressions such as `expect(true)` and `\|\| true` no longer finds matches under `tests/e2e/playwright`. | Replaced no-op assertions with real UI assertions; later QA-004 removed the runtime skip branches that remained after this first cleanup. | 2026-05-19 |
| QA-001 | Rust test linting | All-target clippy with warnings-as-errors currently reports test-target lint debt, while production clippy passes. | Added explicit policy: production clippy is the hard gate, test-target clippy warnings are advisory until test lint policy tightens. | 2026-05-19 |
